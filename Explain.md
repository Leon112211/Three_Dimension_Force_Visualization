# Explain — 标定对比实验的验证机制说明

> 本文档解释 `TDF_DataProcess/ML_Calibration/` 实验报告中"**报告值 vs 独立复算**"这套验证机制:
> 两列数字是什么、为什么要算两遍、0.7% 偏差从哪来、以及"限采 200 帧"等术语的含义。
> 配套阅读:[REPORT.md](TDF_DataProcess/ML_Calibration/REPORT.md)(完整实验报告)、
> [results/results.json](TDF_DataProcess/ML_Calibration/results/results.json)(全部原始记录)。

---

## 1. 先把术语说清楚

### 1.1 帧(frame)

**一帧 = 原始 CSV 里的一行 = 传感器某一瞬间的一次读数**(x1, y1, z1 三个磁场值,单位 uT)。
采集时每个力级稳压保持一段时间、连续采样,所以一个文件有几百行——它们都是**同一个力**下的
重复读数,只是带噪声在抖。数据规模:Z 轴每对传感器 41 个力级、约 2 万多帧。

### 1.2 RMSE(均方根误差)

衡量"预测值和真实值平均差多少":误差先平方(消正负)、取平均、再开根号(单位变回牛顿)。
读法:`Z-H4 线性模型 RMSE = 0.585 N` = 用线性矩阵测 H4 的 Z 向力,典型偏差约半牛。
RMSE 必须配量程读:0.28 N 对 20 N 满量程 = 1.4%FS。它对大误差更敏感(先平方),
"偶尔大错"比"经常小错"更拉高 RMSE——这正是工程上想要的性质。

### 1.3 限采 200 帧

只发生在**训练侧**:每个训练力级随机抽最多 200 帧入训练集(不足则全要);
**测试力级的帧全部保留,一帧不丢**。两个原因:

1. **信息饱和**——同一力级的几百帧 ≈ 同一个值 + 噪声,200 帧已足够估计均值与噪声分布,
   再多只是重复,徒增数倍训练时间;
2. **力级等权**——文件行数从 271 到 1001 不等,不限采则录得久的力级在损失函数里
   占几倍话语权;限采等于给每个力级"一人一票"。

公平性保证:抽样用固定种子(每折 = 1000 + 折号),可复现;同一折内**四个模型共用
同一批被抽中的帧**。

---

## 2. 两列数字是什么

报告中的复现对照表,两列是**同一个物理量算了两遍**——"留出力级上的力重建 RMSE(N)",
区别只在**由谁算出来的**:

| 组合 | 我的报告 (N) | 独立复算 (N) | 相对偏差 |
|---|---|---|---|
| Z-H4 · 线性模型 | 0.5854 | 0.583 | ~0.4% |
| Z-H4 · 二次多项式 | 0.3378 | 0.3355 | ~0.7% ← 最差 |
| Z-H6 · 线性模型 | 0.1928 | 0.1929 | ~0.05% |
| Z-H6 · 二次多项式 | 0.1280 | 0.1271 | ~0.7% |

- **纵向**看行与行 → 实验结论本身(如 H4:0.585 → 0.338,二次模型显著更好);
- **横向**看左右两列 → 数字的可信度(两套独立实现最差只差 0.7%)。

只复算线性/二次,是因为它们是**确定性模型**(无随机种子,理应对上,对不上就是有 bug);
MLP 带随机性,逐位复现无意义,其可信度由种子间波动(±0.02~0.08 N)单独度量。

---

## 3. 两遍分别怎么算的:协议相同、实现独立

这正是"复算"有意义的原因:**协议相同保证可比,实现不同才能揪出代码 bug**。

### 3.1 相同的部分(协议层,两边必须一致)

| 项 | 约定 |
|---|---|
| 数据 | 同一批原始 CSV(Z 轴 41 力级逐帧数据) |
| 清洗 | 剔除每个文件末尾的均值行(记录仪追加的统计行,不是真样本) |
| 输入定义 | ΔB = 逐帧读数 − 零力级(0N 文件)均值,3 通道 |
| 划分 | 4 折按力级交叉:内部级按"序号 mod 4"轮流留出,端点(0N/20N)恒在训练集 |
| 评估 | 留出力级的**全部**帧算 RMSE,4 折取平均 |

### 3.2 不同的部分(实现层,故意独立)

| 项 | 我的报告(`train_calibration.py`) | 独立复算(净室脚本) |
|---|---|---|
| 工具库 | scikit-learn | 纯 numpy,禁用 sklearn、禁止复用实验代码 |
| 数据读取 | 复用管线的 `read_raw_csv()` | 自行重写读取与均值行剔除(同规则、独立实现) |
| 训练帧 | 每力级限采 200 帧 | 全部训练帧,不采样 |
| 线性模型 | `LinearRegression` | `np.linalg.lstsq` 手解最小二乘 |
| 二次模型 | `StandardScaler → PolynomialFeatures(2) → Ridge(α=1e-3)` 管道 | 手工构造标准化二阶特征 + 手解岭回归 |

打个比方:同一道题,一个人用计算器、另一个人手算,约好同样的题目和评分规则,各自独立做——
答案对上了,才敢说答案是对的,而不是计算器按错了恰好好看。

---

## 4. 0.7% 是什么、不是什么

- **不是**模型精度(那是 RMSE / %FS 的事);
- **不是**人为设定的阈值(事先设定的可接受容差是 15%);
- **是**四组对照中**实测的最差相对偏差**——有界的诚实表述(报最坏值,不报最好值)。

微小偏差的来源明确:主要是"限采 200 帧 vs 全部帧"的训练集差异,加上两个库数值路径的
细微不同。**反过来讲**:如果实验代码里存在数据泄漏、划分写错、单位错误这类实质性 bug,
两套异源实现不可能咬合在 1% 以内,会直接差出一截——这就是该机制的检错原理。

| 数字 | 回答的问题 | 性质 |
|---|---|---|
| RMSE 0.28 N(1.4%FS) | 模型测力有多准? | 精度指标 |
| 复现偏差 ≤0.7% | 报告里的数字可信吗? | 可复现性证据 |

---

## 5. 除数值外还核验了什么

- **对抗性代码审查**(独立执行,含实际运行审计):按力级划分无泄漏、基线仅来自恒在
  训练集的零力级、标准化仅在训练集拟合、折覆盖无重复无遗漏、零测试集调参、
  四模型同折同帧——逐项通过;
- **两条定性结论**独立成立:H4 的线性级均值误差在 3–6 N 区间超过 1 N;
  H6 全程不超过 0.5 N。

---

## 6. 一句话总结

**先约定同一套实验协议,再用两套互不参考的实现各算一遍,以最差 0.7% 的偏差互相咬合——
数字因此不依赖任何单一份代码的正确性。** 这是论文级可复现性(reproducibility)的
微缩实践,也是"用 AI 做科研,但给 AI 的产出上质检"的具体形态。
